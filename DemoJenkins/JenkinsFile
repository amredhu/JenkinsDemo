pipeline{
	agent any
		stages{
			stage('BUILD'){
				steps{
						sh 'mvn clean install'
				}
			}
			stage('Testing Stage'){
				steps{
						sh 'mvn test'
				}
			}
			stage('Deploy'){
				steps{
						sh 'mvn deploy'
				}
			}
		}
	
}